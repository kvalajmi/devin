# 🚀 دليل التشغيل السريع

## خطوات التشغيل في 3 دقائق

### 1. التأكد من المتطلبات ✅
```bash
# التحقق من Node.js (يجب أن يكون 16+ )
node --version

# التحقق من npm
npm --version
```

### 2. تثبيت المكتبات 📦
```bash
# الانتقال إلى مجلد المشروع
cd "harmuni exl"

# تثبيت المكتبات (إذا لم تكن مثبتة)
npm install
```

### 3. تشغيل المشروع 🎯
```bash
# تشغيل الخادم المحلي
npm run dev
```

### 4. فتح المتصفح 🌐
- افتح المتصفح واذهب إلى: `http://localhost:5173`
- أو استخدم الرابط الذي سيظهر في Terminal

## 🎨 الصفحات المتاحة

1. **لوحة التحكم** - `/` 
   - إحصائيات عامة ورسوم بيانية

2. **تفاصيل العميل** - `/client/:id`
   - معلومات شاملة للعميل
   - إدارة الدفعات والمرفقات

3. **المستثمرين** - `/investors`
   - قائمة المستثمرين

4. **معاملات المستثمرين** - `/investor-transactions`
   - سجل المعاملات

## ⚡ أوامر مفيدة

```bash
# تشغيل المشروع
npm run dev

# بناء للإنتاج
npm run build

# معاينة النسخة المبنية
npm run preview
```

## 🔧 في حالة وجود مشاكل

### مشكلة: Port مستخدم
```bash
# إيقاف العملية على Port 5173
lsof -ti:5173 | xargs kill -9
```

### مشكلة: مكتبات ناقصة
```bash
# إعادة تثبيت المكتبات
rm -rf node_modules package-lock.json
npm install
```

### مشكلة: cache
```bash
# تنظيف cache
npm run dev -- --force
```

## 📱 التجربة الأولى

1. افتح لوحة التحكم لرؤية الإحصائيات
2. اضغط على "تفاصيل العميل" لأي عميل
3. جرب إضافة دفعة جديدة
4. ارفع مرفق واعرضه
5. جرب طباعة التقارير

---

**🎉 مبروك! النظام جاهز للاستخدام**

# إصلاحات استيراد Excel - تحديث 2024

## المشاكل التي تم حلها ✅

### 1. مشكلة كود المعاملة
**المشكلة السابقة:**
- كان النظام يحاول استخراج كود المعاملة من ملف Excel
- كان يبحث في أعمدة خاطئة
- لم يكن يضيف أرقام تسلسلية

**الحل المطبق:**
- إنشاء كود المعاملة تلقائياً: `k + 5 أرقام + الرقم المتسلسل`
- مثال: `k10001-1`, `k10002-2`, `k10003-3`
- الكود يتم إنشاؤه بجانب اسم العميل

### 2. مشكلة قراءة الدفعات
**المشكلة السابقة:**
- كان يبحث في الأعمدة الخاطئة للدفعات
- كان يقرأ من `startCol + 1, +2, +3` بدلاً من `startCol + 4, +5, +6`

**الحل المطبق:**
- تصحيح الأعمدة: الدفعات في `startCol + 4, +5, +6` (نفس أعمدة المصروفات)
- المبلغ في العمود الخامس للعميل (نفس عمود البيان في المصروفات)
- التاريخ في العمود السادس للعميل (نفس عمود المبلغ في المصروفات)
- الملاحظات في العمود السابع للعميل (نفس عمود التاريخ في المصروفات)

### 3. مشكلة قراءة المصروفات
**المشكلة السابقة:**
- كان يبحث في أعمدة ثابتة `U, V, W`
- لم يكن يتكيف مع موقع العميل

**الحل المطبق:**
- تصحيح الأعمدة: المصروفات في `startCol + 4, +5, +6`
- البيان في العمود الخامس للعميل
- المبلغ في العمود السادس للعميل
- التاريخ في العمود السابع للعميل

### 4. مشكلة قراءة أتعاب المحاماة
**المشكلة السابقة:**
- كان يبحث في أعمدة ثابتة `U, V, W`
- لم يكن يتكيف مع موقع العميل

**الحل المطبق:**
- تصحيح الأعمدة: أتعاب المحاماة في `startCol + 4, +5, +6`
- البيان في العمود الخامس للعميل
- المبلغ في العمود السادس للعميل
- التاريخ في العمود السابع للعميل

## كيفية عمل النظام الجديد

### 1. اكتشاف العملاء
```typescript
// البحث عن أعمدة بداية العملاء
for (let i = 0; i < jsonData[0].length; i++) {
  const cellValue = jsonData[0]?.[i]
  if (cellValue && String(cellValue).includes('كود المعاملة')) {
    clientStartColumns.push(i)
  }
}
```

### 2. إنشاء كود المعاملة
```typescript
// إنشاء كود المعاملة: k + 5 أرقام + الرقم المتسلسل
const baseTransactionCode = `k${String(10000 + clientIndex + 1).slice(1)}`
const transactionCode = `${baseTransactionCode}-${clientIndex + 1}`
// مثال: k10001-1, k10002-2, k10003-3
```

### 3. قراءة البيانات حسب موقع العميل
```typescript
// الدفعات: startCol + 4, +5, +6 (نفس أعمدة المصروفات)
// المصروفات: startCol + 4, +5, +6
// أتعاب المحاماة: startCol + 4, +5, +6 (الصف 39 فقط)
```

**ملاحظة مهمة:** جميع البيانات (الدفعات، المصروفات، أتعاب المحاماة) تستخدم نفس الأعمدة:
- العمود الخامس للعميل: المبلغ/البيان
- العمود السادس للعميل: التاريخ/المبلغ  
- العمود السابع للعميل: الملاحظات/التاريخ

## الفوائد من الإصلاحات

1. **دقة أكبر**: قراءة البيانات من الأعمدة الصحيحة
2. **مرونة**: يتكيف مع أي عدد من العملاء
3. **تنظيم**: كود معاملة فريد لكل عميل
4. **سهولة**: لا حاجة لتعديل الكود لكل ملف Excel

## ملاحظات مهمة

- تأكد من أن ملف Excel يحتوي على عنوان "كود المعاملة" في الصف الأول
- الدفعات يجب أن تكون في الصفوف 15-43
- المصروفات في الصفوف 23-27
- أتعاب المحاماة في الصف 39 فقط
- النظام يتكيف تلقائياً مع موقع العميل في الملف
